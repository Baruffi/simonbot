export const defaultPrefix = 'simon ';
// TODO: Plans for support of multiple prefixes or maybe even regex patterns. As it is currently, it is always possible to instantiate a new interpreter for each prefix.
